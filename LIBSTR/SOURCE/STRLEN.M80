        name    strlen
        public  strlen

        cseg


;------------------------------------------------------------------------
; возвращает длину строки
;------------------------------------------------------------------------
; на входе:
;    BC     - строка
; на выходе:
;    HL     - длина строки
strlen:
        lxi     H, 0
    @slenl:
        ldax    B
        ora     A
        rz
        inx     B
        inx     H
        jmp     @slenl
end
